//started

#include <stdio.h>
#include <stdlib.h>

typedef struct node {
  int data;
  struct node * next;
} NODE, *PNODE, **PPNODE ;                   // With this we are integrating both typedef and the struct node

int main (){

  PNODE Head = NULL;

  return 0;
}
